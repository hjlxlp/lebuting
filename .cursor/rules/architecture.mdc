---
description: lebuting 架构、Hermes 接入、业务模块 slug、8181 API、.env 与部署。改 backend、新模块、Hermes/Skills、网络配置时读。
globs: backend/**/*,utils/**/*.uts,vite.config.js,.env.example,scripts/sync-android-config.mjs
alwaysApply: false
---

# lebuting 架构

App（uni-app x）+ 笔记本 Hermes（:8642）+ 业务 FastAPI（:8181，SQLite）。详述见 `my/docs/lebuting-开发部署指引.md`、Hermes 见 `my/docs/Hermes-WSL-完整部署指南.md`。

## 三条路径

1. **App 对话** → `POST /v1/chat/completions`（Bearer `VITE_HERMES_API_KEY`）→ Agent + Skills
2. **微信 Bot** → Hermes Gateway → 同上 Skills
3. **App 直连 UI** → `GET/POST /api/{slug}/...`（不经对话）

## 网络

| 服务 | H5 | App |
|------|-----|-----|
| Hermes | `/hermes-api` | `VITE_HERMES_HOST:8642` |
| 业务 | `/biz-api` | `VITE_BIZ_API_HOST:8181` |

- Host 填 **backend 所在机器局域网 IP**（WSL 镜像下用 Windows IP，禁 `127.0.0.1` 真机）
- 改 `.env` → H5 重启；App 重编；Android 跑 `sync-android-config.mjs`

## 业务模块（slug 前后端统一、模块隔离）

| 模块 | slug | API | 表 | 前端 |
|------|------|-----|-----|------|
| 吃什么菜 | `eat-dish` | `/api/eat-dish` | `eat_dish_*` | `pages/eat-dish/`、`utils/eat-dish.uts` |
| 做什么菜 | `cook-dish` | `/api/cook-dish` | `cook_dish_*` | `pages/cook-dish/`、`utils/cook-dish.uts` |

后端：`backend/app/{slug_underscore}/`（`db.py` `router.py` `schemas.py`），`main.py` 挂载 `/api/{slug}`。

## 新模块 checklist

slug → 后端包+表 → `utils/{slug}.uts` → `pages/{slug}/` → `pages.json` → 首页入口

## 关键路径

`utils/hermes.uts` · `backend/main.py` · `backend/data/lebuting.db` · `.cursor/skills/*/SKILL.md`

## 约定

- 页面只调 `utils/`，不硬编码 URL
- 本地 backend：`conda activate lebuting` 后 `uvicorn main:app --host 0.0.0.0 --port 8181`
- 业务 SQLite 与 Hermes `state.db` 分离
