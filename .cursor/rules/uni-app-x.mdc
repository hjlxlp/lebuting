---
description: uni-app x / UTS 写法约束（Promise、HTTP 解析、CSS、三端网络）。编辑 .uvue/.uts 或前端编译报错时读。
globs: "**/*.{uvue,uts}"
alwaysApply: false
---

# uni-app x / UTS

[概述](https://doc.dcloud.net.cn/uni-app-x/) · [UTS](https://doc.dcloud.net.cn/uni-app-x/uts/) · [CSS 子集](https://doc.dcloud.net.cn/uni-app-x/css/) · [编译器已知问题](https://doc.dcloud.net.cn/uni-app-x/uts/compiler-known-issues.html)

**UTS ≠ Web TS**：Android→Kotlin、iOS→Swift；按原生规则写，勿用 JS 习惯。

## 结构

- 页面 `pages/**/*.uvue` + `<script setup lang="uts">`
- 网络/逻辑 `utils/*.uts`；**禁止** `.uvue` 硬编码 URL
- 模块 slug 见 `architecture.mdc`

## 网络（参考 `utils/hermes.uts`、`utils/eat-dish.uts`）

```uts
// #ifdef H5
export const BIZ_BASE_URL = '/biz-api'
// #endif
// #ifndef H5
export const BIZ_BASE_URL = 'http://' + host + ':' + port
// #endif
```

## UTS 必守

| 项 | 规则 |
|----|------|
| Promise | `new Promise` + resolve/reject；禁 `return req().then(() => v)` |
| HTTP body | `as UTSJSONObject` |
| HTTP 响应 | `dataText = JSON.stringify(raw['data'])` → `JSON.parse` |
| 禁止 | `as unknown`、对象字面量 `as` 自定义 type |
| 回调 | `setTimeout(() => { ... }, ms)` 必须块 |
| CSS | 禁 `%` 宽度；用 `px`；边框写全 |

## 自检

H5 → Android 真机 → iOS 编译；列表/提交/统计各点一遍
